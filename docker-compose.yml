version: '2'
services:
  nginx-proxy:
    image: jwilder/nginx-proxy
    ports:
    - 80:80
    - 443:443
    volumes:
    - ~/.minipage/certs:/etc/nginx/certs:rw
    - /etc/nginx/vhost.d
    - /usr/share/nginx/html
    - /var/run/docker.sock:/tmp/docker.sock:ro

  letsencrypt-agent:
    image: jrcs/letsencrypt-nginx-proxy-companion
    volumes:
    - /var/run/docker.sock:/var/run/docker.sock:ro
    volumes_from:
    - nginx-proxy

networks:
  default:
    external:
      name: minipage-network
